{
  "name": "vega-probe",
  "version": "0.2.0",
  "description": "Agent-neutral control and test evidence for the Vega Virtual Device",
  "type": "module",
  "bin": {
    "vega-probe": "bin/vega-probe.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "SKILL.md"
  ],
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check bin/vega-probe.mjs && node --check src/*.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
